<UserControl x:Class="CatClipComposer.Controls.NumericEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="120">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Slider x:Name="ValueSlider" Margin="1,0,1,3"
                IsSnapToTickEnabled="True"
                ValueChanged="ValueSlider_ValueChanged" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="28" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="28" />
            </Grid.ColumnDefinitions>
            <Button Padding="0" Click="Decrease_Click" Content="−"
                    AutomationProperties.Name="Decrease value" />
            <TextBox x:Name="ValueTextBox" Grid.Column="1" Margin="3,0"
                     HorizontalContentAlignment="Center"
                     TextChanged="ValueTextBox_TextChanged" />
            <Button Grid.Column="2" Padding="0" Click="Increase_Click" Content="+"
                    AutomationProperties.Name="Increase value" />
        </Grid>
    </Grid>
</UserControl>
