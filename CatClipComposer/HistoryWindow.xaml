<Window x:Class="CatClipComposer.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export history"
        Width="900"
        Height="600"
        MinWidth="720"
        MinHeight="460"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="Export history" />
            <TextBlock x:Name="StatusTextBlock"
                       Margin="0,5,0,0"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="Loading…" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5">
                <ListBox x:Name="HistoryListBox" ItemsSource="{Binding History}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Width="315" Margin="8">
                                <TextBlock FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Entry.OutputPath}"
                                           Text="{Binding OutputFileName}" />
                                <TextBlock Margin="0,4,0,0"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           Text="{Binding CreatedText}" />
                                <TextBlock Margin="0,2,0,0"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           Text="{Binding SummaryText}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Column="2"
                    Padding="16"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="5">
                <Grid DataContext="{Binding SelectedItem, ElementName=HistoryListBox}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock FontSize="16"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               Text="{Binding Entry.OutputPath}" />
                    <TextBlock Grid.Row="1"
                               Margin="0,6,0,12"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="Clips used in this compilation" />
                    <ListBox x:Name="HistoryClipListBox"
                             Grid.Row="2"
                             ItemsSource="{Binding Entry.Clips}"
                             MouseDoubleClick="HistoryClipListBox_MouseDoubleClick">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="7">
                                    <TextBlock>
                                        <Run Text="{Binding Order, StringFormat=# {0}  }" />
                                        <Run FontWeight="SemiBold" Text="{Binding FileName}" />
                                    </TextBlock>
                                    <TextBlock Margin="26,3,0,0"
                                               FontSize="11"
                                               Foreground="{StaticResource MutedTextBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding FullPath}"
                                               Text="{Binding FullPath}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <DockPanel Grid.Row="2" Margin="0,16,0,0" LastChildFill="False">
            <Button DockPanel.Dock="Right" MinWidth="90" IsCancel="True" Content="Close" />
            <Button DockPanel.Dock="Right"
                    MinWidth="115"
                    Margin="0,0,8,0"
                    Click="OpenOutput_Click"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Content="Show output" />
        </DockPanel>
    </Grid>
</Window>
